

## Plan: Rename "Shift" to "Clock In"

Two files, two label changes:

1. **`src/pages/StartPage.tsx`** — Change mode toggle label from `"Shift"` to `"Clock In"`
2. **`src/components/SessionConflictDialog.tsx`** — Update `MODE_LABELS`: `shift: "Clock In"`

No other changes.

